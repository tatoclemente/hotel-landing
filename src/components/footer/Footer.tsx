import { Container } from "@mui/material";

export const Footer = () => {
  return (
    <footer>
      <Container
        sx={{ backgroundColor: "lightsalmon", minHeight: "332px", mx: 0, minWidth: '100%' }}
      >
        <h1>Hello Page</h1>
      </Container>
    </footer>
  );
};
